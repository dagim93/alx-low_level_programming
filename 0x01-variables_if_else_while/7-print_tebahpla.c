#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Print the lowercase alphabet in order
 * Return: 0
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%i", a);
		a++;
	}

	putchar('\n');
	return (0);
}
